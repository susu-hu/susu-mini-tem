<!--pages/effects/3dClound/index.wxml-->
<view class="tagBall">
  <view class="tag" wx:for="{{tagEle}}" wx:key="index" style="opacity:{{item.o}};z-index:{{item.z}};left: {{item.x}}rpx;top: {{item.y}}rpx;transform: scale({{item.s}});color:{{item.color}}">
    {{item.title}}
  </view>
</view>